# In Render, set the service Root Directory to `wwtd/backend` (repo root = wwtd git root).
services:
  - type: web
    name: wwtd-api
    runtime: python
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.8"
      - key: JWT_SECRET
        sync: false
      - key: SMTP_HOST
        sync: false
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASSWORD
        sync: false
      - key: SMTP_FROM
        sync: false
      - key: CORS_ORIGINS
        value: "*"
      # Optional: attach a Render Disk mounted at /var/data, then set:
      # - key: SQLITE_PATH
      #   value: /var/data/wwtd.db
